const menu=document.querySelector('.menu');const nav=document.querySelector('nav');menu.addEventListener('click',()=>nav.classList.toggle('open'));document.querySelectorAll('nav a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));
const form=document.getElementById('interestForm');const success=document.getElementById('success');form.addEventListener('submit',e=>{e.preventDefault();const name=document.getElementById('name').value.trim(),phone=document.getElementById('phone').value.trim(),course=document.getElementById('course').value;if(!name||!/^[0-9]{10}$/.test(phone)||!course){alert('Please enter a valid name, 10-digit mobile number and course.');return}const data=JSON.parse(localStorage.getItem('rankersHubInterest')||'[]');data.push({name,phone,course,date:new Date().toISOString()});localStorage.setItem('rankersHubInterest',JSON.stringify(data));success.style.display='block';form.reset();});
